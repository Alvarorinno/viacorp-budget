# Variables de entorno para producción (Railway)
NODE_ENV=production
PORT=3001

# Ruta del archivo SQLite (Railway: usar /data/database.db con volumen montado)
DB_PATH=/data/database.db

# Clave secreta para JWT — cambiar en producción
JWT_SECRET=cambia_esta_clave_secreta_2026

# Contraseñas de usuarios (opcional, si no se definen usa los valores por defecto)
DIRECTOR_PASS=dir2026
FINANZAS_PASS=fin2026
